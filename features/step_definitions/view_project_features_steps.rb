Given /^the user has selected the "([^"]*)" project$/ do |project|
  pending # express the regexp above with the code you wish you had
end

Given /^the "([^"]*)" project has features$/ do |project|
  pending # express the regexp above with the code you wish you had
end

When /^they vist the feature list page$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^they should see the following features:$/ do |table|
  # table is a Cucumber::Ast::Table
  pending # express the regexp above with the code you wish you had
end
